ingress:
  enabled: "true"
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/secure-backends: "true"
  hosts:
    - dashboard.legion-dev.gcp.epm.kharlamov.biz
  tls:
    - secretName: kubernetes-dashboard-certs
      hosts:
        - dashboard.legion-dev.gcp.epm.kharlamov.biz