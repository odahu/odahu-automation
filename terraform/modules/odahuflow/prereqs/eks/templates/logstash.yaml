input {
  s3 {
    bucket            => "${bucket}"
    region            => "${region}"
    access_key_id     => "${access_key_id}"
    secret_access_key => "${secret_access_key}"
    codec             => "json"
    interval          => 30
    prefix            => "logs"
  }
}
