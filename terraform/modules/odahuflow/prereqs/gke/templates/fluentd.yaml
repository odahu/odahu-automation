output:
  target: "gcs"
  gcs:
    bucket: "${data_bucket}"
    authorization: "GWI"
serviceAccount:
  annotations:
    iam.gke.io/gcp-service-account: "${collector_sa}"
fluentd:
  resources: 
    limits:
      cpu: "${cpu_limits}"
      memory: "${memory_limits}"
    requests:
      cpu: "${cpu_requests}"
      memory: "${memory_requests}"

